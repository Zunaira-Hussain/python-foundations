# file_io

Reading from and writing to files in Python.

| Topic | Description |
|-------|-------------|
| `open()` | Reading and writing text files |
| `with` | Context managers (automatic cleanup) |
| JSON | Parsing and generating JSON data |
| Exceptions | Handling `FileNotFoundError` etc. |

*Implementations in progress — see roadmap.*